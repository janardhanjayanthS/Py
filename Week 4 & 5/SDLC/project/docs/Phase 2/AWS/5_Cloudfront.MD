# AWS Cloudfront:

- It is a CDN used for caching static (img, JS, CSS) or dynamic (video, APIs) contents
- **CDN:** a geographically distributed group of servers that caches content close to end users.

