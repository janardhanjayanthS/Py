CMD:

- run: docker init -> to generate .dockerignore, Dockerfile, compose.yaml file
- run: docker compose up --build -> to create a container, build and start the application - can see print stmt in terminal
